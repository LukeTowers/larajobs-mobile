Title: The SPA Way (React/Vue)
Layout: Code Block
Body:
const [query, setQuery] = useState('');
const [jobs, setJobs] = useState([]);

useEffect(() => {
    axios.get(`/api/jobs?q=${query}`)
        .then(res => setJobs(res.data));
}, [query]);

Layout Guidelines:
- **Alignment:** Center Title, Code Block below.
- **Visual Hierarchy:**
  1.  **Title:** Medium-Large (40pt), white.
  2.  **Code Block:** Centered, syntax-highlighted code in a rounded container with a "Code Panel Background" (#1E293B) and a "Problem Red" (#FB7185) border or glow to subtly indicate this is the "Old/Hard" way.
- **Background Details:**
  - Solid Deep Slate (#0F172A).
- **Syntax Highlighting:**
  - `const`, `useState`, `useEffect`: #C084FC (Purple 400).
  - `axios.get`, `setJobs`: #38BDF8 (Blue 400).
  - `'/api/jobs'`: #4ADE80 (Green 400).
  - `[]`: #FACC15 (Yellow 400).
Design Style:
- Background: #0F172A (Deep Slate)
- Accent: #FB7185 (Problem Red)

Speaker Notes:
So we grew up. We went full SPA. We built robust REST APIs and consumed them with React or Vue. This is cleaner code, but look at the cost. I now have to manage loading states, fetch logic, and duplicate my validation rules on the client. I am now maintaining two separate applications.
