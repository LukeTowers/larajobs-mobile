Title: The Code (Real-time Search)
Layout: Two Columns
Column 1:
class JobsPage extends Component {
    public $query = ''; // <--- State

    public function render() {
        return view('jobs', [
            'jobs' => Job::query()
                ->where('title', 'like', $this->query)
                ->get()
        ]);
    }
}
Column 2:
<input wire:model.live="query">

Layout Guidelines:
- **Alignment:** Split Screen (50/50).
- **Visual Hierarchy:**
  1.  **Column 1 (PHP):** Syntax-highlighted PHP code in a "Code Panel" (#1E293B) with a blue/purple border. Label it "Server (PHP)".
  2.  **Column 2 (HTML):** Syntax-highlighted HTML code in a "Code Panel" (#1E293B) with a green/yellow border. Label it "Browser (HTML)".
  3.  **Connection:** Draw a glowing, animated-style arrow or line connecting `public $query` in PHP to `wire:model="query"` in HTML. Label the arrow "Live Binding".
- **Background Details:**
  - Solid Deep Slate (#0F172A).
- **Syntax Highlighting:**
  - `wire:model.live`: #F472B6 (Livewire Pink) - Make this pop!
  - `public $query`: #38BDF8 (Blue 400).
Design Style:
- Background: #0F172A (Deep Slate)
- Accent: #F472B6 (Livewire Pink)

Speaker Notes:
This is the Livewire way. I define a public property $query. I bind it to the input with wire:model.live. That’s it. I don't need an event listener. I don't need a fetch request. When the user types, Livewire automatically updates the property on the server. My PHP class is the frontend state. It's just PHP!
