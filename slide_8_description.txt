Title: The Inertia.js Way
Layout: Code Block
Body:
import { router } from '@inertiajs/react'

const handleSearch = (e) => {
    router.get('/jobs',
        { search: e.target.value },
        { preserveState: true }
    )
}

Layout Guidelines:
- **Alignment:** Center Title, Code Block below.
- **Visual Hierarchy:**
  1.  **Title:** Medium-Large (40pt), white.
  2.  **Code Block:** Centered, syntax-highlighted code in a rounded container with a "Code Panel Background" (#1E293B) and a "Problem Red" (#FB7185) border or glow to subtly indicate this is still part of the "Old/Hard" way (even if Inertia is better, it's still JS context switching).
- **Background Details:**
  - Solid Deep Slate (#0F172A).
- **Syntax Highlighting:**
  - `import`, `const`, `return`: #C084FC (Purple 400).
  - `router.get`: #38BDF8 (Blue 400).
  - `'/jobs'`: #4ADE80 (Green 400).
  - `{ preserveState: true }`: #FACC15 (Yellow 400) - Highlight this to emphasize the mental overhead.
Design Style:
- Background: #0F172A (Deep Slate)
- Accent: #FB7185 (Problem Red)

Speaker Notes:
Inertia made this infinitely better. It glued our monolith back together. But... look at the code. I am still writing a JavaScript handler. I still have to think about preserveState. I still have to mentally shift gears between PHP and JS just to filter a list.
